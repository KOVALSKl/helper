import { createTheme } from '@mui/material/styles';

export const helperTheme = createTheme({
    palette: {
        primary: {
            main: '#14a433'
        },
        secondary: {
            main: '#000000'
        }
    }
})